<% content_for :title, "This dashboard link has expired" %>

<%= render "shared/frame", max_width: :form do %>
  <%= render "shared/page_header", title: "This dashboard link has expired." %>

  <section class="rogue-card">
    <p class="rogue-paragraph">Dashboard links are valid for 8 days from the moment we send them.</p>
    <p class="rogue-paragraph">Watch for your next weekly digest — it'll include a fresh link.</p>
  </section>
<% end %>
