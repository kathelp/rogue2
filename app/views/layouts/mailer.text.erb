<%= yield %>
<% if (dev_url = dev_conductor_reply_url(message: @_message)) %>

----------------------------------------
DEV ONLY: reply via Action Mailbox conductor (form pre-filled):
<%= dev_url %>
<% end %>
