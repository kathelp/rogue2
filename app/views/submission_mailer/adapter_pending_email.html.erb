<!DOCTYPE html>
<html>
  <body style="font-family: -apple-system, BlinkMacSystemFont, sans-serif; max-width: 600px; margin: 40px auto; padding: 0 20px; color: #222;">
    <h1 style="font-size: 22px;">No action needed yet</h1>
    <p>Hi,</p>
    <p>
      <strong><%= @tenant.dealership_name %></strong> picked
      <strong><%= @method.to_s.upcase %></strong>
      as the way to send their <%= @request.metric_key.to_s.tr("_", " ") %> data.
      We're still finishing the adapter for that path.
    </p>
    <p>We'll be in touch when it's ready — no action is needed from you right now.</p>
    <p>— The Rogue team</p>
  </body>
</html>
