Hi,

<%= @tenant.dealership_name %> picked <%= @method.to_s.upcase %> as the way to send their <%= @request.metric_key.to_s.tr("_", " ") %> data. We're still finishing the adapter for that path.

We'll be in touch when it's ready — no action is needed from you right now.

— The Rogue team
