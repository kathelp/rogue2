This one is overdue — <%= @tenant.dealership_name %>'s <%= @request.metric_key.to_s.tr("_", " ") %> for <%= @prompt.scheduled_for.in_time_zone(@tenant.time_zone).strftime("%B %Y") %> hasn't come in. Please send it as soon as you can — the longer it sits the more we have to chase.
