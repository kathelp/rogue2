A friendly heads up — <%= @tenant.dealership_name %>'s <%= @request.metric_key.to_s.tr("_", " ") %> for <%= @prompt.scheduled_for.in_time_zone(@tenant.time_zone).strftime("%B %Y") %> is due in a few days. No need to scramble — drop the number in when you have a minute.
