Hi,

<%= @tenant.gm_name %> at <%= @tenant.dealership_name %> named you as the person to handle this:

  <%= @question.prompt %>

To finish setup (about a minute), open this link:

  <%= @setup_url %>

You'll pick how you want to send the data (web form, CSV, or API), and we'll let you know when your first submission is due.

— The Rogue team
